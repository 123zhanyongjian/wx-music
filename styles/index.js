/**
 * 样式系统入口
 * 统一导出所有样式配置
 */
const theme = require('./theme');
const typography = require('./typography');
const spacing = require('./spacing');

module.exports = {
  theme,
  typography,
  spacing
};

